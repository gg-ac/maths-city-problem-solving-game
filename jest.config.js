module.exports = {
    preset: 'ts-jest', // Use ts-jest preset
    testEnvironment: 'node', // Specify the test environment
    testMatch: ['**/?(*.)+(spec|test).[jt]s?(x)'], // Specify test file patterns
  };